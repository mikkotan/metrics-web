import styled from 'styled-components'

const Card = styled.div`
  padding: 24px;
  margin: 16px;
  background: #f9fafb;
  border-radius: 8px;
  box-shadow: 0 3px 10px rgb(0 0 0 / 0.2);
`

export default Card
